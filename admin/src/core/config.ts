import { PLUGIN_ID } from '../pluginId';

export default {
  pluginId: PLUGIN_ID,
  uuid: {
    modules: {
      menu: 'menu',
      attribute: 'attribute',
      parameter: 'parameter',
      slug: 'slug',
      translation: 'translation',
      template: 'template'
    }
  }
}